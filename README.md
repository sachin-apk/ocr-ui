# OCR UI

A web-based UI to extract text from images using OCR.

## Features
- Upload image
- Extract text
- Copy/download text

## Tech Stack
- React / TailwindCSS
- Space ocr / API

## Setup
cd "/backend"
npm install
node index.js
cd ..
cd "/handwritten"
npm install
npm run dev
